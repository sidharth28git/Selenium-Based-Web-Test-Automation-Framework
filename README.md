# HealthcareAutomation

Java + Selenium + TestNG + Maven framework targeting the CURA demo site.

## Prerequisites
- Java 17+
- Maven
- Chrome

## Run
```bash
mvn clean test
```
Reports at `target/surefire-reports/index.html`.

## CI
GitHub Actions workflow in `.github/workflows/ci.yml`.
